<h1>{{title}}</h1>
{{#if author_list.length}}
<ul>
  {{#each author_list}}
    <li>
      <a href="{{this.url}}">{{this.name}} ({{this.date_of_birth_formatted}} - {{this.date_of_death_formatted}})</a>
    </li>
  {{/each}}
</ul>
{{else}}
<p>There are no authors.</p>
{{/if}}
